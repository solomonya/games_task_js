/**
 * Sanitizes the name of a game.
 * - Trims whitespace from both ends of the string.
 * - Converts all characters to lower case.
 * @param {string} name - The original game name.
 * @returns {string} The sanitized game name.
 */
export function sanitizeName(name) {
  // Implementation needed
}

/**
 * Sanitizes the price of a game.
 * - Removes all non-numeric characters except for the decimal point.
 * - Converts the resulting string to a floating-point number.
 * @param {string} price - The original price as a string.
 * @returns {number} The sanitized price as a number.
 */
export function sanitizePrice(price) {
  // Implementation needed
}

/**
 * Validates if the name of the game is unique in the provided list of games.
 * @param {string} name - The name to validate.
 * @param {Array} games - The list of all games.
 * @returns {Object} - Result object with validity and reason.
 */
export function validateUniqueName(name, games) {
  // Implementation will be added here.
}

/**
 * Validates if the price after discount does not become negative.
 * @param {number} price - The original price of the game.
 * @param {number} discount - The discount to apply.
 * @returns {Object} - Result object with validity and reason.
 */
export function validatePriceWithDiscount(price, discount) {
  // Implementation will be added here.
}
